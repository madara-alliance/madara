# TODOs:

## Servers :
  ### Create a dependency graph.
    Each server will inform the setup about it's pre-requsities and server should validate that before starting the respective server.
  ### Kill on drop ?
    There is a param `kill_on_drop` in the command which is set to `false` by default,
    Check if it's good to use ?
