mod madara_factory {
    mod contract;
    mod interface;
}

mod EthBridgeEIC {
    mod contract;
    mod interface;
}

mod account {
    // mod component;
    mod contract;
    // mod new_component;
// mod utils;
}

mod interfaces {
    pub mod bridge;
    pub mod replaceable;
    pub mod roles;
}
